# The GCP project identifier to use
project_id  = "my-gcp-project"

# The Compute Engine regions where the resources will be created
region = ["us-west1", "us-east1"]

# Optional labels to add to resources
labels = {
    "owner" = "tester-name"
}

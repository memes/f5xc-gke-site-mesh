# Autogenerated by foundations Terraform
# spell-checker: disable
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../service-discovery

namePrefix: ${name}-
commonAnnotations: ${jsonencode(annotations)}
commonLabels: ${jsonencode(labels)}

patches:
  - target:
      group: ''
      version: v1
      kind: Secret
      name: service-discovery
      namespace: kube-system
    patch: |-
      - op: replace
        path: /metadata/annotations/kubernetes.io~1service-account.name
        value: ${name}-service-discovery

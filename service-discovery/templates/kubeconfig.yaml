# Autogenerated by service-discovery Terraform for use by F5 XC Kubernetes
# service discovery.
# spell-checker: disable
---
apiVersion: v1
kind: Config
current-context: ${name}
clusters:
  - name: ${name}
    cluster:
      certificate-authority-data: ${ca_cert}
      server: ${endpoint}
contexts:
  - name: ${name}
    context:
      cluster: ${name}
      user: ${sa}
users:
  - name: ${sa}
    user:
      token: ${token}
